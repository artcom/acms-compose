version: "3.2"

services:
  api:
    image: "artcom/git-json-api:latest"
    environment:
      - REPO_URI=http://content-repo:80/git/content/
      - BODY_SIZE_LIMIT=1mb
    depends_on:
      - content-repo
    ports:
      - 81:3000

  asset-server:
    build: ./git-json-cms-asset-server
    volumes:
      - type: volume
        source: assets
        target: /var/www/webdav
    ports:
      - 82:80

  content-repo:
    build: ./git-json-cms-content-repo
    volumes:
      - type: volume
        source: content
        target: /srv/content
    ports:
      - 83:80

  frontend:
    build:
      context: ./git-json-cms-frontend

volumes:
  content:
  assets:
