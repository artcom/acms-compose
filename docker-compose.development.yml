version: "3.2"

services:
  cms:
    build:
      context: ./cms
      dockerfile: Dockerfile.development
    volumes:
      - ./cms:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 8080:8080
